spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/db_billing_users
    username: billing
    password: qwerty123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true


server:
  port: 8082
